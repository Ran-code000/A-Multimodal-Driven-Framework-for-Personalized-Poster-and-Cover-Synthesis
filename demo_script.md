# Demo 演示视频录制脚本与流程

> **时长目标：** 5分00秒 -- 5分30秒  
> **录制工具：** OBS Studio / Windows 自带录屏 (Win+Alt+R) / Bandicam  
> **分辨率：** 1920×1080，清晰显示代码与UI  
> **录制环境：** 提前启动 Gradio WebUI（`python app.py`），确认模型已加载完毕  

---

## 录制前准备清单

| # | 准备项 | 说明 |
|---|--------|------|
| 1 | **启动 WebUI** | `python app.py`，等待控制台输出 `Pipeline loaded successfully` |
| 2 | **打开浏览器** | 访问 `http://localhost:7860` |
| 3 | **准备素材文件** | 确认 `test1.jpg` ~ `test4.jpg` 风格参考图存在 |
| 4 | **打开图片浏览器** | 预先打开 `evaluation_v2_output/` 文件夹，以便展示16组结果 |
| 5 | **打开 metrics.json** | 用 VS Code 打开 `evaluation_v2_output/metrics.json`，用于定量结果环节 |
| 6 | **准备PPT/演讲稿** | 打开本脚本作为提词器 |
| 7 | **关闭通知** | 关闭微信/QQ/邮件等弹窗通知 |
| 8 | **设置麦克风** | 测试音量，确保声音清晰无杂音 |
| 9 | **清理桌面** | 隐藏无关窗口，只保留浏览器、VS Code、终端 |

---

## 完整录制流程（5分30秒）

---

### 第 0 段：开场封面 (0:00 -- 0:10)

**画面：** 展示 final_report.pdf 封面页（TikZ蓝色封面）

**旁白：**
> "大家好，我是崔敬然，学号23336055。本demo展示的是图文驱动的个性化封面与海报生成系统——一个基于SDXL扩散模型、融合ControlNet布局控制与IP-Adapter风格迁移的多模态海报生成框架。"

**操作：** 打开 `final_report.pdf`，停留2秒在封面，然后切换到浏览器 Gradio 界面。

---

### 第 1 段：系统简介与三模态输入范式 (0:10 -- 1:00)

**画面：** Gradio WebUI 界面全景 + 切换至系统架构图

**旁白：**
> "本系统的核心创新在于三模态协同可控生成。传统的文生图模型——比如Stable Diffusion——只能通过文本提示词来控制生成内容，构图和风格完全不可控。
>
> 我们的系统引入了三种模态的输入：
> - **文本模态**：用户输入海报主题，例如'古风美人海报'，系统通过DeepSeek大语言模型自动增强为高质量的SDXL提示词；
> - **布局模态**：用户可以上传一张手绘草图，系统通过Canny边缘检测提取结构信息，再由ControlNet以弱约束方式引导构图——注意我们用的是弱约束策略，权重仅0.15到0.55，保留模型的创作自由度；
> - **风格模态**：用户上传一张风格参考图，系统通过CLIP视觉编码器提取色彩和纹理特征，由IP-Adapter注入SDXL的交叉注意力层，实现零样本风格迁移。
>
> 三者在SDXL UNet内部的不同层级和注意力机制上独立作用、互不冲突。"

**操作：**
1. 展示 Gradio 界面全景（左右双栏布局）
2. **切换画面**：打开 `final_report.pdf` 翻到系统架构图那一页（`fig:architecture`）
3. 用鼠标在架构图上指三个输入路径
4. **切回浏览器**

---

### 第 2 段：WebUI 操作演示 —— 完整工作流 (1:00 -- 2:30)

**画面：** Gradio WebUI，全程操作录屏

#### 2a. 输入主题 + 提示词增强 (1:00 -- 1:25)

**旁白：**
> "现在演示完整的操作流程。首先在左侧输入海报主题——我用'古风美人'主题来演示。在文本框中输入：'一位身着汉服的古典仕女，桃花背景下，工笔风格，柔和金色光线'。"

**操作：**
1. 在 Theme 文本框输入：`一位身着汉服的古典仕女，桃花背景下，工笔风格，柔和金色光线`
2. 点击 **"Enhance Prompt via DeepSeek"** 按钮
3. 等待1-2秒，展示增强后的英文SDXL提示词

**旁白：**
> "点击增强按钮后，DeepSeek大模型在约1-2秒内将简短的中文主题扩展为40-80词的英文SDXL提示词，包含了构图、光线、色彩、材质、氛围等多维度的艺术描述。注意结尾自动添加了'masterpiece, best quality, highly detailed, sharp focus'等质量增强词。"

#### 2b. 上传布局草图 + 风格参考图 (1:25 -- 1:50)

**旁白：**
> "接下来上传布局草图和风格参考图。布局草图告诉ControlNet海报的空间结构——这里我用的是程序生成的古风美人构图：中央人物+四角花卉框架+顶部横幅区域。"

**操作：**
1. 点击 Layout Sketch 区域，选择一张布局草图
2. **切画面**：快速展示 `gen_v2.py` 中 `make_layout("ancient-beauty")` 代码（约5秒）
3. **切回Gradio**

**旁白：**
> "风格参考图定义了目标视觉风格——暖色调的古典人物画纹理。这个纹理图也是程序化生成的，通过IP-Adapter将色彩调性迁移到生成结果中。"

**操作：**
1. 点击 Style Reference 区域，选择 `test1.jpg`（古风美人风格参考图）
2. 用鼠标指一下 Advanced Settings 中的 ControlNet Weight (0.55) 和 IP-Adapter Weight (0.40)

#### 2c. 生成海报 (1:50 -- 2:15)

**旁白：**
> "参数保持默认——ControlNet权重0.55、IP-Adapter权重0.40、推理步数30步、CFG Scale 7.5、随机种子42。点击Generate Poster开始生成。"

**操作：**
1. 点击 **"Generate Poster"** 按钮
2. 展示生成过程（约15-25秒等待）

**旁白（等待期间）：**
> "生成过程大约需要15到25秒。在这段时间里，系统正在进行以下步骤：
> - Canny边缘检测提取布局草图的结构特征；
> - CLIP视觉编码器提取风格参考图的特征嵌入；
> - ControlNet零卷积支路将布局条件注入SDXL UNet编码器层；
> - IP-Adapter解耦交叉注意力将风格特征注入指定层；
> - SDXL UNet在文本+布局+风格三重条件的引导下进行25步DPM-Solver++扩散去噪；
> - VAE解码器将潜在表示解码为1024×1024的RGB图像。"

3. 生成完成，展示结果

**旁白：**
> "生成完成。可以看到，生成结果准确呈现了古风美人的暖黄工笔色调，人物居中构图，符合布局草图的结构指引。下方的VRAM状态信息显示显存占用约6-8GB，远在16GB安全范围内。"

#### 2d. 演示纯文本对比 (2:15 -- 2:30)

**旁白：**
> "为了对比，我们再看一下纯文本模式的效果。清空布局草图和风格参考图，将ControlNet权重和IP-Adapter权重都设为0，重新生成。"

**操作：**
1. 清除上传的图片
2. 将 ControlNet Weight 和 IP-Adapter Weight 都调为 0
3. 点击 Generate Poster
4. 等待生成，展示结果

**旁白：**
> "可以看到，纯文本生成的结果虽然也符合提示词的基本语义，但构图随机、色彩中性、缺乏风格统一性。这就是多模态控制的直观价值。"

---

### 第 3 段：对照实验展示 —— G1 vs G4 视觉对比 (2:30 -- 3:20)

**画面：** 切换到 `evaluation_v2_output/` 文件夹，展示4×4汇总图和各主题对比网格

**旁白：**
> "我们设计了4组对照实验来系统量化每种控制模块的贡献：
> - G1：纯文本基线——不使用ControlNet和IP-Adapter
> - G2：仅ControlNet布局控制
> - G3：仅IP-Adapter风格控制
> - G4：完整多模态系统——ControlNet + IP-Adapter联合使用
>
> 覆盖四个视觉主题：古风美人、青绿山水、赛博朋克、春日花卉。"

**操作：**
1. **打开 `summary_grid.png`**（4×4主汇总对比图），全屏展示

**旁白：**
> "这是4×4的汇总对比图。行对应四个主题，列对应四个实验组。从左到右观察每一行：
> - 第一列G1：纯文本生成的构图和色彩都比较随机；
> - 第二列G2：ControlNet使构图规范化——古风美人居中、山水层次分明；
> - 第三列G3：IP-Adapter使色彩协调——暖黄工笔、青蓝山水、紫青霓虹、粉黄花卉；
> - 第四列G4：完整系统同时具备准确构图和鲜明风格。"

**操作：**
2. **依次打开四个主题的2×2对比网格**：`grid_ancient-beauty.png`, `grid_green-landscape.png`, `grid_cyberpunk.png`, `grid_spring-flowers.png`

**旁白：**
> "以赛博朋克为例，G1的霓虹元素分布随机，G2增加了透视感，G3强化了紫青霓虹色调，G4同时呈现精确透视和霓虹氛围——构图与色彩的协同控制效果显著。"

---

### 第 4 段：定量结果与指标分析 (3:20 -- 4:10)

**画面：** 切换到 VS Code，打开 `evaluation_v2_output/metrics.json` + 展示报告中的表格

**旁白：**
> "定量评测采用三个指标：CLIP Score衡量图文语义匹配度，简化FID衡量生成分布变化，美学启发式指标基于边缘密度和色彩饱和度。"

**操作：**
1. 打开 `metrics.json`，用鼠标指一下关键数据
2. **切换画面**：打开 `final_report.pdf` 翻到定量指标表格页面（表 `tab:group_avg`）

**旁白：**
> "核心结果：完整多模态系统G4的平均CLIP Score为0.341，相比纯文本基线G1的0.304提升12.2%。美学启发式指标从2.83提升到3.53，提升24.7%。主观评测中，美观度从2.7提升到4.1，提升超过50%。
>
> 值得注意的是：G4的增益——12.2%——非常接近G2增益4.6%与G3增益7.9%的线性叠加12.5%。这一数据表明，布局控制与风格控制在CLIP语义空间中具有近似正交的贡献维度——两者互不干扰，协同增效。
>
> FID最高的是G4，值3.47。这不是说G4质量更差——恰恰相反，FID衡量的是分布差异而非绝对质量。G4的FID最高，说明ControlNet+IP-Adapter联合作用对生成分布的重塑最为显著，而这种重塑是正向的。"

---

### 第 5 段：技术亮点与工程优化 (4:10 -- 4:40)

**画面：** VS Code展示 `engine.py` 核心代码 + 终端展示VRAM状态

**旁白：**
> "在工程实现上，本系统有三个关键技术亮点：
>
> **第一，弱约束控制策略。** ControlNet条件权重仅0.15到0.55，远低于默认值1.0。这避免了'填色画'式的过度约束——在提供构图引导的同时，保留了SDXL的创造性空间。
>
> **第二，消费级GPU全流程适配。** SDXL+ControlNet+IP-Adapter完整加载需要超过20GB显存，但我们通过CPU卸载、VAE切片/瓦片、float16精度等组合优化，将其压缩到16GB VRAM——也就是一张RTX 5060 Ti——上稳定运行。"

**操作：**
1. 展示 `engine.py` 中 `_apply_memory_optimizations()` 部分代码
2. 切到终端，展示 VRAM 信息（`nvidia-smi` 或 WebUI 底部状态栏）

**旁白：**
> "**第三，模块化流水线架构。** 五大核心模块——LLM提示词增强、Canny边缘检测、ControlNet布局注入、IP-Adapter风格注入、SDXL扩散去噪——通过明确接口耦合，支持独立调试与灵活组合。例如，不需要风格控制时，传入中性灰图并将IP-Adapter scale设为0即可。"

---

### 第 6 段：总结与展望 (4:40 -- 5:10)

**画面：** 切回封面页或汇总图

**旁白：**
> "总结一下：
> - 本系统实现了一种以SDXL为基座、整合ControlNet和IP-Adapter的图文驱动多模态海报生成框架；
> - 文本、布局、风格三模态协同可控，CLIP Score提升12%，美学指标提升25%；
> - 弱约束策略平衡了构图引导与创作自由度；
> - 通过CPU卸载等优化，在16GB消费级GPU上实现1024×1024海报的稳定推理。
>
> 未来工作方向包括：集成文字渲染能力实现海报标题生成、构建风格LoRA库支持WebUI热插拔切换、探索LCM-LoRA将推理加速至近实时交互。
>
> 感谢观看！完整的代码、实验数据和结题报告已开源在GitHub仓库中。"

**操作：**
1. 切回封面
2. 展示 GitHub 仓库地址

---

### 第 7 段：收尾 (5:10 -- 5:30)

**画面：** 黑屏或封面

**旁白：**
> "以上是图文驱动的个性化封面与海报生成系统的完整演示。谢谢！"

---

## 录制后处理

1. **剪辑**：剪掉开头的准备画面和结尾多余部分
2. **字幕**：可选添加中文字幕（剪映/PR自动识别）
3. **导出**：1080p, 30fps, MP4格式
4. **上传**：上传至B站/YouTube
5. **填写链接**：将链接填入 `submission/demo_video_link.txt`

---

## 时间轴速查表

| 时间段 | 内容 | 画面 |
|--------|------|------|
| 0:00--0:10 | 开场封面 | PDF封面 |
| 0:10--0:40 | 三模态输入范式介绍 | Gradio UI全景 |
| 0:40--1:00 | 系统架构图讲解 | PDF架构图 |
| 1:00--1:25 | 输入主题 + 增强提示词 | Gradio操作 |
| 1:25--1:50 | 上传布局草图 + 风格参考图 | Gradio操作 |
| 1:50--2:15 | 生成海报（含等待过程解说） | Gradio生成过程 |
| 2:15--2:30 | 纯文本对比演示 | Gradio操作 |
| 2:30--2:50 | 4×4汇总图对比 | summary_grid.png |
| 2:50--3:20 | 四主题2×2网格详细对比 | grid_*.png |
| 3:20--3:50 | 定量指标分析（CLIP/FID/Aes） | metrics.json + PDF表格 |
| 3:50--4:10 | 正交性分析与FID解读 | PDF表格 |
| 4:10--4:40 | 技术亮点与工程优化 | engine.py代码 + 终端 |
| 4:40--5:10 | 总结与展望 | 封面/汇总图 |
| 5:10--5:30 | 结束语 | 封面 |

---

## 关键名词中英对照（录制时参考）

| 中文 | English |
|------|---------|
| 扩散模型 | Diffusion Model |
| 潜在扩散模型 | Latent Diffusion Model (LDM) |
| 去噪扩散概率模型 | Denoising Diffusion Probabilistic Model (DDPM) |
| 条件控制网络 | ControlNet |
| 图像提示适配器 | IP-Adapter |
| 交叉注意力 | Cross-Attention |
| Canny边缘检测 | Canny Edge Detection |
| CLIP评分 | CLIP Score |
| Fréchet初始距离 | Fréchet Inception Distance (FID) |
| 无分类器引导 | Classifier-Free Guidance (CFG) |
| DPM求解器 | DPM-Solver++ |
| CPU卸载 | CPU Offload |
| VAE切片/瓦片 | VAE Slicing / VAE Tiling |
| 弱约束控制 | Weak-Constraint Control |
| 零样本风格迁移 | Zero-Shot Style Transfer |
| 人在回路 | Human-in-the-Loop |
